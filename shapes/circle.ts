//Calc circunference perimeter

const PI = Math.PI


export const calcCircunference = (radius:number ):number => (radius * 2) * PI 


//Calc area of a circle

export const calcAreaCircle = (radius:number):number => (radius ** 2) * PI